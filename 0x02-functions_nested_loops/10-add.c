#include "main.h"

/**
* add - a functin that adds two integers and returns the result
*@a: parameter 1
*@b: parameter 2
* Return: always 0 when successful
*/

int add(int a, int b)
{
	return (a + b);
}
