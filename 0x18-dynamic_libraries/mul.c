#include <stdlib.h>
/**
 *mul - a function that rereturns multiplication of number
 *@a: the first parameter
 *@b: second parameter
 *Return: alwys 0 on sucess
 */
int mul(int a, int b)
{
	return (a * b);
}
