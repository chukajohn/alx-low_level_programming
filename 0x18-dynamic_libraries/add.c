#include <stdlib.h>
/**
 *add - a function that rereturns addition of number
 *@a: the first parameter
 *@b: second parameter
 *Return: alwys 0 on sucess
 */
int add(int a, int b)
{
	return (a + b);
}
