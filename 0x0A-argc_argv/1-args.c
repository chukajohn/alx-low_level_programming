#include <stdio.h>
/**
 *main - progame that prints the numb
 *@argc: argument count
 *@argv: argument vector
 *Return: 0 always on success
 */

int main(int argc, char *argv[])
{
	(void)argv;

	printf("%d\n", argc);
	return (0);
}
